### To start, copy the following commands:
```
cd ~/code/${GITHUB_USERNAME}
git@github.com:AshIgnYeo/javascript-reboot.git
cd javascript-reboot
rm -rf .git
```

### After that remember to run
```yarn install```
